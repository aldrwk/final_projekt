<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.final_project.product.productMapper">

<!--    <select id="findById" resultType="productDomain" resultMap="accountMap">-->
<!--        select host_num, account_num, bank_code, account_owner from account where host_num = ${hostNum}-->
<!--    </select>-->

    <insert id="insert" parameterType="productDomain">
        insert into product
        values (null, ${hostNum}, '${title}', '${content}', ${price}, '${address}', '${addressDetail}', ${secCateNum}, ${thumnail}, ${producRegitDate}, ${producUpdateDate})
    </insert>

<!--    <update id="update" parameterType="accountDomain">-->
<!--        update account-->
<!--        set account_num   = ${accountNum},-->
<!--            bank_code     = '${bankCode}',-->
<!--            account_owner = '${accountOwner}',-->
<!--            change_date   = #{changeDate}-->
<!--        where host_num = ${hostNum}-->
<!--    </update>-->



    <resultMap id="productMap" type="productDomain">
        <result column="produc_num" property="producNum" />
        <result column="host_num" property="hostNum" />
        <result column="address_detail" property="addressDetail" />
        <result column="sec_cate_num" property="secCateNum" />
        <result column="produc_regit_date" property="producRegitDate" />
        <result column="produc_update_date" property="producUpdateDate" />
    </resultMap>
</mapper>